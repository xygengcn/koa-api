{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run-Dev",
            "type": "shell",
            "command": "yarn dev",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}